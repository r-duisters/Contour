<?xml version='1.0' encoding='utf-8'?>
<!--
  The four strings that used to live here are generated in app/build.gradle
  instead, because they differ between the standalone build and the LAN
  wrapper. Defining them in both places is a duplicate-resource error, so this
  file is deliberately empty rather than deleted: Capacitor expects it.
-->
<resources>
</resources>
