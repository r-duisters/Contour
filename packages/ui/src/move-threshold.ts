/**
 * What "a big move" means when nobody has said.
 *
 * Five percent in a day is large enough that a diversified portfolio does not
 * hit it weekly, and small enough that it catches the day something actually
 * happened. It lives here rather than in either screen because two of them
 * offer it — the setup flow as a default and the settings screen as the figure
 * it starts from — and a second copy is how they come to disagree.
 */
export const DEFAULT_MOVE_THRESHOLD = 5;
